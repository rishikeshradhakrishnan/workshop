# F2 — Missing ownership check on private reviews (`app/users.py`, CWE-639)

**Patch:** `F2.patch` · **Status:** written, with a caveat · *SAMPLE note shipped with the workshop template*

| Claim | Verdict | Evidence |
|---|---|---|
| Addresses this one finding | yes | adds `if user_id != g.user_id: abort(403)` before the query; imports `g` |
| Introduces no new vulnerability | yes | bare diff re-read: a new deny path only |
| Behaviour otherwise unchanged | yes, for legitimate callers | owners still receive their own private reviews (covered by `tests/test_users.py::test_owner_can_read_own_private_reviews`); cross-user reads now return 403 — that *is* the fix. **No existing test covers the cross-user case: add one** (`ada` requesting `/users/2/reviews/private` → 403) in the same PR. |

**Diffstat:** `app/users.py | 5 +++--` (1 file changed)
**Tests run:** `uv run pytest -q` → 8 passed
**`git apply --check`:** clean against the stamped revision (independent of F1; applies on `main` or on top of F1)
