# F1 — SQL injection in review search (`app/reviews.py`, CWE-89)

**Patch:** `F1.patch` · **Status:** written (verifier vouched for all three claims) · *SAMPLE note shipped with the workshop template*

| Claim | Verdict | Evidence |
|---|---|---|
| Addresses this one finding | yes | the f-string query is replaced by a `?` placeholder with the pattern bound as a parameter; no other statement changes |
| Introduces no new vulnerability | yes | bare diff re-read: no new sinks, no widened inputs, LIKE wildcard semantics unchanged |
| Behaviour otherwise unchanged | yes | benign searches return the same rows; inputs containing quotes now match literally instead of erroring — accepted-input set is not narrowed |

**Diffstat:** `app/reviews.py | 5 +++--` (1 file changed)
**Tests run:** `uv run pytest -q` → 8 passed (project suite; `tests/test_reviews.py::test_search_matches_body_text_and_hides_private` covers the route)
**`git apply --check`:** clean against the stamped revision

Apply: `git switch -c fix/f1-sqli && git apply <report>/patches/F1.patch && uv run pytest -q && git commit -am "Fix SQL injection in review search"` — one PR per patch.
